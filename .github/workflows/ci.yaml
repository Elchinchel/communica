on:
  push:

jobs:
  tests:
    strategy:
      matrix:
        runner: ["ubuntu-latest", "windows-latest"]
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]

    services:
      rabbitmq:
        image: docker.io/rabbitmq:4.2.1-alpine
        ports:
          - 5672:5672

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install nox
        run: pip install nox
        
      - name: Run tests
        run: nox -r -s test-python${{ matrix.python-version }} -- -v

  # TODO
  # test_windows_local_connector:
  #   strategy:
  #     matrix:
  #       python-version: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]

  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/checkout@v5
  #     - name: Set up Python ${{ matrix.python-version }}
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: ${{ matrix.python-version }}

  #     - name: Run tests
  #       run: nox -r -s test-python${{ matrix.python-version }} -- -v -k

